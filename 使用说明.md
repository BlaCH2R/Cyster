# Cyster 使用说明

## 一、准备工作

1. 安装 `Cyster Setup 0.1.0-beta.exe`（或使用 `dist/win-unpacked/Cyster.exe`）。
2. 准备一份关卡素材：`.cytoidlevel` 文件（例如“示例关卡.cytoidlevel”），或者已经解压好的关卡目录（内含 `level.json`、谱面 `chart.*.txt`、音乐、图片等）。

## 二、创建 / 打开项目（欢迎界面）

软件启动后默认进入**欢迎页**（交互逻辑参考 Cylheim），分为两个区域：

- **开始编辑**：新建项目、打开项目、最近项目（支持“在文件夹中显示 / 复制路径 / 移除记录”）。
- **管理当前项目**：项目已打开时显示，可一键返回编辑器或进入项目设置。

工具栏的 🏠 可随时返回欢迎页；📁 可打开当前项目所在文件夹。

### 2.1 新建项目（推荐）

点击 **＋ 新建项目**，在弹出的窗口中：

1. 填写**项目名称**；
2. **选择音乐**（`.mp3` / `.ogg`，必选）；
3. **选择谱面**（`.txt` / `.json`，必选，即 Cytoid 的 chart 文件）；
4. **选择背景**（`.png` / `.jpg` / `.jpeg`，可选）；
5. **选择 StoryBoard**（`.json`，可选；不选则创建空白 StoryBoard）；
6. 点击“创建项目”，指定 `.ctr` 保存位置。

创建时如果目标文件已存在，会提示“打开已有项目 / 覆盖为新项目 / 取消”；如果当前已打开其他项目，会提示“关闭当前项目并在本窗口创建”。

创建后，所选文件会被**复制到项目文件夹**（`.ctr` 所在目录），程序自动生成 `level.json` 并加载，中央预览区立即显示游戏画面。

### 2.2 打开已有项目

点击 **打开项目 (.ctr / .ctdsber)**，选择之前保存的 `.ctr`（或旧版 `.ctdsber`）配置文件即可恢复全部编辑内容。若当前已打开其他项目，会提示“关闭当前项目并在本窗口打开”。

### 2.3 导入现有关卡

点击 **导入 .cytoidlevel**，选择已有的关卡压缩包，程序会解压并把它转换为 `.ctr` 项目后加载。若当前已打开项目，则可以选择“导入到当前项目”，把关卡文件解压并替换到当前项目目录。

> 提示：欢迎界面下方会显示最近打开过的 6 个项目，点击即可快速打开。

## 三、项目设置

顶部 **项目设置** 可随时更换当前项目的音乐、谱面、背景或 StoryBoard 文件；更换后项目会立即重新加载，新文件会被复制进项目文件夹。

## 四、撰写 StoryBoard（核心工作流）

### 4.1 添加对象

点击左侧 **＋ 添加对象**，选择类型：

| 类型 | 用途 |
| --- | --- |
| Sprite | 显示关卡内图片（双击素材库中的图片可快速创建） |
| Text | 显示文本（支持 `富文本`：`<b>` `<i>` `<size=60>` `<color=#ff0000>`） |
| Video | 播放视频（实验性，建议 H.264 的 .mp4） |
| Line | 绘制由线段组成的图形（三角形、扫描线等） |
| Controller | 控制整个场景：相机、扫描线、颜色、全部滤镜 |
| Note Controller | 控制谱面中的 Note：位置、旋转、颜色、透明度、大小等 |

### 4.2 时间轴操作

- 每个对象占一条轨道，色块为对象**片段**，菱形为**关键帧**。
- 点击片段或菱形选中对象/关键帧；右侧属性面板随之刷新。
- **拖动菱形**修改关键帧时间（仅支持数字时间；`start:$note` 等相对时间请在属性面板编辑）。
- **拖动片段**整体平移该对象所有数字时间。
- 拖动顶部**播放头**或点击标尺可跳转时间；`空格` 播放/暂停。
- 右侧“＋ 在播放头添加关键帧”会在当前播放头位置复制一个关键帧；“复制当前帧”把当前帧复制到播放头。

### 4.3 编辑属性

右侧属性面板分为三部分：

1. **对象**：`id`、`parent_id`（父对象，如 `note_controller_1` 可实现跟随 Note 移动）、`target_id`（控制同类型对象）、`note`（Note 选择器 JSON）。
2. **关键帧列表**：初始状态与各关键帧，点击切换编辑目标，可删除关键帧。
3. **状态属性**：按对象类型列出文档支持的全部字段。

常用技巧：

- Sprite 初始 `opacity` 为 0（不可见），记得先设为 1 或添加状态。
- `layer`：0 = 背景之上、Note 之下；1 = Note 之上、UI 之下；2 = 最上层。同层用 `order` 排序。
- 坐标为 800×600 舞台坐标系（中心 0,0，右上 400,300）；可直接填数字，也可用 `noteX:0.5`、`stageY:600`、`cameraX:1` 等换算。
- 缓动函数与官方 easings.net 一致（含 `none`、`linear`、各 In/Out/InOut、`spring`、`blink`、bounce/back/elastic 等）。

### 4.4 预览

中央预览是**按当前时间实时求值**的游戏画面：背景、Note（含扫描线、Hold 尾、Flick、Drag 链）、StoryBoard 对象与滤镜都会随播放头变化。勾选“滤镜”可临时关闭后处理以提升流畅度。

## 五、保存与导出

1. **保存**：把 StoryBoard 写为标准 JSON 到关卡目录，并登记到 `level.json`。
2. **导出 StoryBoard JSON**：同“保存”，可手动复制该文件用于提交或分享。
3. **导出 .cytoidlevel**：保存后把整个关卡目录打包为 `.cytoidlevel` 关卡包。

导出文件为**规范化 JSON**：无注释、无尾逗号、字段按官方规范，可直接被 Cytoid / CytoidPlayer 加载。

## 六、快捷键

| 按键 | 功能 |
| --- | --- |
| `空格` | 播放 / 暂停 |
| `←` / `→` | 步进 0.05 秒 |
| `Shift + ←` / `Shift + →` | 步进 0.5 秒 |
| 双击素材库图片 | 创建 Sprite |

## 七、常见问题

**Q：为什么预览和真实游戏画面略有不同？**
A：预览为按官方引擎语义与官方贴图实现的 2D 渲染，部分滤镜（径向模糊、鱼眼、冲击波等）为近似实现。

**Q：安装包被 Windows 提示风险？**
A：安装包未做代码签名，属正常提示，选择“仍要运行”即可。

**Q：想手动微调 JSON？**
A：使用“导入 StoryBoard JSON”可加载外部文件（支持注释与尾逗号的宽松 JSON），编辑后再导出为规范 JSON。
